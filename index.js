/* 
// เชื่อมต่อกับฐานข้อมูล
use myDatabase

// สร้างคอลเลกชันโดยใช้คำสั่ง createCollection
db.createCollection("Collection1", {
  capped: true,
  size: 1000000,
  max: 1000
})

// ตรวจสอบคอลเลกชันที่มีอยู่ในฐานข้อมูล
show collections

// สร้างคอลเลกชันโดยการแทรกเอกสาร
db.Collection1.insertOne({
  name: "John Doe",
  age: 30
})

// ตรวจสอบคอลเลกชันอีกครั้ง
show collections


1. เริ่มต้นด้วยการเชื่อมต่อกับฐานข้อมูลที่ต้องการใช้งานโดยใช้คำสั่ง `use` ตามด้วยชื่อฐานข้อมูล
2. สร้างคอลเลกชันโดยใช้คำสั่ง `createCollection` และระบุชื่อคอลเลกชันเป็นพารามิเตอร์แรก
   - ในตัวอย่างนี้ เรากำหนดให้เป็น capped collection ด้วย `capped: true`
   - กำหนดขนาดสูงสุดของคอลเลกชันเป็น 1,000,000 ไบต์ ด้วย `size: 1000000`
   - กำหนดจำนวนเอกสารสูงสุดในคอลเลกชันเป็น 1,000 เอกสาร ด้วย `max: 1000`
3. ตรวจสอบคอลเลกชันที่มีอยู่ในฐานข้อมูลโดยใช้คำสั่ง `show collections`
4. สร้างคอลเลกชันใหม่โดยการแทรกเอกสารลงในคอลเลกชันที่ยังไม่มีอยู่ เช่น `db.myNewCollection.insertOne()`
   - ในตัวอย่างนี้ เราแทรกเอกสารที่มีฟิลด์ `name` และ `age` ลงในคอลเลกชัน `myNewCollection`
5. ตรวจสอบคอลเลกชันอีกครั้งโดยใช้คำสั่ง `show collections` เพื่อดูว่าคอลเลกชันใหม่ถูกสร้างขึ้นแล้ว

โค้ดนี้แสดงให้เห็นวิธีการสร้างคอลเลกชันใน MongoDB ทั้งโดยใช้คำสั่ง `createCollection` และการแทรกเอกสารลงในคอลเลกชันที่ยังไม่มีอยู่ เราสามารถกำหนดตัวเลือกต่างๆ สำหรับคอลเลกชันได้ตามต้องการ และตรวจสอบคอลเลกชันที่มีอยู่ในฐานข้อมูลได้ด้วยคำสั่ง `show collections`
*/
